<%= t('mailboxer.message_mailer.header_new', subject: @subject ) %>
===============================================

<%= t('mailboxer.message_mailer.new_message') %>

-----------------------------------------------
<%= @message.body.html_safe? ? @message.body : strip_tags(@message.body) %>
-----------------------------------------------

<%= t('mailboxer.message_mailer.visit') %> <%= root_url %> <%= t('mailboxer.message_mailer.more_info') %>
